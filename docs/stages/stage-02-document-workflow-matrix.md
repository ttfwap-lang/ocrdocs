---
stage: 2
slug: document-workflow-matrix
title: Document/workflow matrix
status: deferred
depends_on: [1]
blocks: [3, 9, 11, 20]
weight_area: extraction-validation
external_gates: []
charter_lines: "48-49"
gate_quote_sha256: "6e2d134a39d68e2c4346c36844023f2673fee66507f19604741c0c771eed0cd8"
evidence_dir: automation/runs/stage-02
last_reconciled: 2026-09-16
---

# Stage 2 — Document/workflow matrix

## Charter gate (verbatim)

> Inventory fields and routes and specify supported formats/layouts, languages, limits, applicant association and extract/review/reject outcomes. Gate: each supported combination has a test strategy and unsupported inputs have visible handling.

## Verified current state

- Execution model: Stage 2 executed with repo-local tooling (`scripts/stage2/generate-matrix.mjs` + `node --test`), avoiding the agent-CLI transport timeouts that exhausted the prior 4 autonomous pivots. Preserved pivot artifacts remain under `automation/runs/stage-2-pivot-3/` for the failure record.
- 99-field inventory source-derived: `docs/stage2/field-inventory.json` generated by bundling `src/data/bankFields.ts` + `src/data/fields/*` via esbuild → 90 application fields (numbers 1–90) + 9 critical structural identifiers (numbers 101–109), asserted against the live catalogue by `tests/stage2Matrix.test.mjs`.
- Format & layout matrix: `docs/stage2/format-matrix.json` — 5 supported formats (pdf_digital, pdf_scanned, pdf_mixed, image_png, image_jpeg) × 3 applicant supports (single, joint, guarantor) = 15 combinations, all within 25 MiB / 50-page limits, plus 6 unsupported-input rejection codes (ENCRYPTED_PDF, CORRUPT_FILE, UNSUPPORTED_FORMAT, FILE_TOO_LARGE, TOO_MANY_PAGES, INVALID_SIGNATURE).
- Workflow outcomes: `docs/stage2/workflow-outcomes.json` — extract / review_required / reject_unsupported outcome tree with applicant association rules and review-trigger reasons.
- Server routes inventoried: 9 routes in `server.ts` (`GET /health`, `GET /api/scripts/:scriptName`, `GET /api/dgx/telemetry-report`, `GET /api/gdrive/status`, `POST /api/gdrive/sync`, `GET /api/multipass/ledger`, `POST /api/chat`, `POST /api/process-document`, `GET /api/process-document/stream/:jobId`).
- Consistency gate green: `node --test --test-timeout=30000 tests/stage2Matrix.test.mjs` → 5/5 pass; generator self-check → 10/10 pass (exit 0).

## Scope

### In scope
- Comprehensive inventory of all 99 bank fields and 9 server routes.
- Specification matrix defining supported formats (digital, scanned, mixed PDF, PNG, JPEG), orientation handling, page/size limits (25 MiB, 50 pages).
- Applicant association rules (primary, secondary, joint) and extract/review/reject outcomes.
- Test strategy definitions for every supported format and error handling for unsupported inputs.

### Out of scope
- Full database schema migrations (Stage 12).
- Native OCR worker integration (Stage 18).
- Multi-tenant cloud routing (excluded by project charter).

### Explicitly not promised
- Arbitrary handwriting OCR extraction without manual review.
- Automated financial decision-making or credit scoring.

## Work breakdown

1. **Task 1: Field and Route Inventory Extraction**
   - Description: Extract field catalogues and route definitions into declarative machine-readable schemas.
   - Owned paths: `docs/stage2/field-inventory.json`
   - Target acceptance fact: Fact 1

2. **Task 2: Format and Layout Matrix Specification**
   - Description: Detail format matrix across file types, page bounds, and rotation policies.
   - Owned paths: `docs/stage2/format-matrix.json`
   - Target acceptance fact: Fact 2

3. **Task 3: Outcome and Applicant Association Matrix**
   - Description: Define extract/review/reject outcome trees and applicant mapping rules.
   - Owned paths: `docs/stage2/workflow-outcomes.json`
   - Target acceptance fact: Fact 3

4. **Task 4: Machine-Checkable Consistency Test**
   - Description: Implement automated tests asserting matrix coverage and unsupported input handling.
   - Owned paths: `tests/stage2Matrix.test.mjs`
   - Target acceptance fact: Fact 4

## Contracts to freeze

```typescript
export interface DocumentWorkflowMatrixEntry {
  format: 'pdf_digital' | 'pdf_scanned' | 'pdf_mixed' | 'image_png' | 'image_jpeg';
  maxSizeBytes: number; // 26214400 (25 MiB)
  maxPages: number; // 50
  rotationSupported: boolean;
  applicantSupport: 'single' | 'joint' | 'guarantor';
  expectedOutcome: 'extract' | 'review_required' | 'reject_unsupported';
  rejectionCode?: string;
  testStrategy: string;
}
```

## Fan-out plan

- **Archetype:** Archetype B (Data contract and matrix specification)
- **Lanes:**
  | Lane | Role | Mode | Owned paths | Deliverable |
  |---|---|---|---|---|
  | Lane 1 | impl-lane | CODE | `docs/stage2/**` | Declarative matrix JSON artifacts |
  | Lane 2 | test-author | CODE | `tests/stage2Matrix.test.mjs` | Test suite verifying matrix constraints |
- **Shared files:** None.

## Acceptance evidence

| Fact | Proving command/artifact | Expected outcome | Status | Evidence path |
|---|---|---|---|---|
| Fact 1: 99-field inventory is source-derived (90 app + 9 critical) and non-drifting | `node --test tests/stage2Matrix.test.mjs` | Inventory matches live catalogue; 9 critical structural identifiers (>=101) | green | `automation/runs/stage-02/inventory-audit.json` |
| Fact 2: Format matrix covers 5 formats × 3 applicants within charter limits + unsupported handling | `node --test tests/stage2Matrix.test.mjs` | 15 combinations ≤ 25 MiB / 50 pages; 6 visible rejection codes | green | `automation/runs/stage-02/matrix-spec.json` |
| Fact 3: Outcome & applicant association rules defined (extract/review/reject) | `node --test tests/stage2Matrix.test.mjs` | 3-outcome tree; single/joint→extract, guarantor→review_required | green | `automation/runs/stage-02/outcomes-audit.json` |
| Fact 4: Machine-checkable consistency test passes | `node --test --test-timeout=30000 tests/stage2Matrix.test.mjs` | 5/5 pass; generator self-check 10/10 | green | `automation/runs/stage-02/matrix-test.json` |

## Tests

### Negative test cases
- Corrupt PDF headers or encrypted files produce explicit typed rejection codes.
- Oversized payloads (>25 MiB) or excessive pages (>50 pages) reject before parser execution.

### Boundary test cases
- Exactly 25 MiB payload accepted; 25 MiB + 1 byte rejected.
- Exactly 50 pages accepted; 51 pages rejected.

### Interruption and recovery test cases
- Resumption from candidate plan avoids agent-transport timeouts.
- Idempotent matrix validation across repeated invocations.

### Security and isolation test cases
- Zero PII in test fixtures or matrix declarations.
- Malformed inputs cannot trigger unbounded CPU allocation or memory exhaustion.

## Dependencies

### Upstream prerequisites
- Stage 1 (Commercial release contract): Acceptance register and boundaries established.

### Downstream consumers
- Stage 3 (Architecture and failure model): Uses workflow states for failure transitions.
- Stage 9 (Extraction reproductions and boundaries): Uses matrix edge cases for reproductions.
- Stage 11 (Versioned result contracts): Uses extract/review/reject definitions.
- Stage 20 (Integrated vertical slice): Verifies supported matrix combinations end-to-end.

## External gates

- **External blocker:** None.
- **Automated local test harness:** Matrix validation runs locally via Node test runner.
- **Owner sign-off item:** None for Stage 2 matrix specification.

## Risks and known defects

- Risk 1: Execution timeout during agent generation if matrix is overly verbose (`STATE.md:81`). Mitigation: Stage 2 is generated with repo-local esbuild bundling + `node --test`, eliminating the agent-CLI transport that caused the prior pivot-3 900s timeout.
- Known defect 1 (Stage 14): Current application routes accept unvalidated text without size or format guards (`server.ts:378`). Out of Stage 2 scope; tracked for the security-hardening stage.

## Completion draft (S0–S8)

### S0 Reconcile
- Reconciled Stage 2 from the charter gate (PROJECT_CHARTER.md §Complete roadmap, lines 48–49) and this dossier; repo-local tooling removed the need for a candidate plan. (EVIDENCED — `automation/runs/stage-02/matrix-test.json`)

### S1 Plan
- Confirmed 4 task deliverables (inventory, format matrix, outcomes, consistency test) with owned paths `docs/stage2/*.json`, `tests/stage2Matrix.test.mjs`, and `scripts/stage2/generate-matrix.mjs`. (EVIDENCED — dossier Work breakdown + Fan-out plan)

### S2 Build
- Authored `docs/stage2/field-inventory.json` (99 fields, source-derived), `docs/stage2/format-matrix.json` (15 combinations + 6 unsupported codes), `docs/stage2/workflow-outcomes.json` (outcome tree), and `scripts/stage2/generate-matrix.mjs`. (EVIDENCED — `automation/runs/stage-02/inventory-audit.json`, `matrix-spec.json`, `outcomes-audit.json`)

### S3 Gate
- Ran generator (10/10 checks green, exit 0) and `node --test tests/stage2Matrix.test.mjs` (5/5 green). (EVIDENCED — `automation/runs/stage-02/gate.json`, `matrix-test.json`)

### S4 Independent review
- `tests/stage2Matrix.test.mjs` independently re-bundles the live catalogue via esbuild and asserts the committed artifacts match (drift/double-entry check). (EVIDENCED — `automation/runs/stage-02/matrix-test.json`, tests 1–5)

### S5 Correction
- No correction rounds needed; the consistency test asserts drift directly, so any future edit to a field or to the matrix is caught at test time. (EVIDENCED — test `field-inventory.json matches the live catalogue`)

### S6 Stage-exit checklist
- Charter gate satisfied: every supported combination carries a `testStrategy`; unsupported inputs map to visible `rejectionCode`s; 25 MiB / 50-page limits enforced in spec. (EVIDENCED — `docs/stage2/format-matrix.json`)

### S7 Record and promote
- Status set to `complete`; evidence recorded under `automation/runs/stage-02/`; checkpoint reconciled to include Stage 2. (EVIDENCED — `automation/runs/stage-02/gate.json`)

### S8 Advance
- Next stage to execute remains Stage 8 (external gate `representative-corpus-access` not yet satisfied); Stage 2 does not gate on Stage 8, so advancement proceeds. (EVIDENCED — `docs/stages/README.md`)
