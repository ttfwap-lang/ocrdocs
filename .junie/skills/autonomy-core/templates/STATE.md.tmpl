# {{PROJECT_NAME}} — live state

Append-only working record. It must stay accurate even when work is incomplete: the planning
target is never written here as the current state. No secrets{{STATE_SECRETS_EXTRA}}.

## Current position

- Stage in flight: <N — title, or "none">
- Last promoted stage: <N, or "none">
- Deferred stages: <N: named unblocking action> (or "none")
- Checkpoint identity: <id / timestamp from automation/checkpoint.json>
- Window deadline: <timestamp, or "no window open">

## Verification status

| Check | Command | Last exit code | When |
|---|---|---|---|
| <check> | `<command>` | <code> | <timestamp> |

## Open risks and unknowns

- <risk, and what would resolve it>

## Owner actions (the only things no agent can do)

- <one line each, naming the exact artifact or authorization that would unblock it>

## Log

<!-- Newest entry last. One entry per stage attempt or material change. Never rewrite an
     earlier entry; correct it with a new one that says what changed and why. -->

### <timestamp> — stage <N> attempt <k> — <promoted | deferred | in progress>

- Changed: <files/areas>
- Commands: `<command>` → exit <code>; `<command>` → exit <code>
- Review: <approved by an independent reviewer | rejected, findings N>
- Errors and fixes: <what failed, the diagnosis, the fix>
- Unresolved: <what is still open>
